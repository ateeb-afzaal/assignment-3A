import React from "react";

const ResearchAreaBox = ({ area }) => {
  return (
    <span className="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-medium">
      {area}
    </span>
  );
};

export default ResearchAreaBox;
